# Dependencies for running an LLM through the emitted BF16 kernels, on top of
# fpy2[triton] (torch and triton) and ../requirements.txt:
#
#   pip install -r requirements.txt
transformers>=4.51     # Qwen3
datasets               # WikiText-2 (Phase 2)
lm-eval[hf]>=0.4       # zero-shot suite (Phase 3)
